type Todo {
    id: ID!
    text: String!
    completed: Boolean!
}

type Query {
    todos: [Todo!]!
}

input NewTodo {
    text: String!
}

type Mutation {
    createTodo(input: NewTodo!): ID!
    markTodoAsComplete(input: ID!): Boolean!
}
