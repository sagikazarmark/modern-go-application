type TodoItem {
    id: ID!
    title: String!
    completed: Boolean!
}

type Query {
    todoItems: [TodoItem!]!
}

input NewTodoItem {
    title: String!
}

type Mutation {
    addTodoItem(input: NewTodoItem!): ID!
    markTodoAsComplete(input: ID!): Boolean!
}
