type Todo {
    id: ID!
    title: String!
    completed: Boolean!
}

type Query {
    todos: [Todo!]!
}

input NewTodo {
    title: String!
}

type Mutation {
    createTodo(input: NewTodo!): ID!
    markTodoAsComplete(input: ID!): Boolean!
}
